# Versions to upgrade

- Rust version in `rust-toolchain.toml`.
- Rust dependencies in `Cargo.toml`.
- pre-commit dependencies in `.pre-commit-config.yaml`.
- Versions of actions in `.github/workflows/ci.yml`.
- Versions of CI dependencies in `.github/workflows/ci.yml`.
