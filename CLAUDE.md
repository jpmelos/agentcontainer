# Development Tools

- Run linters by running `./scripts/dev_lint.sh`.
  - That script has the same interface as `pre-commit`.
- Run tests by running `./scripts/dev_tests.sh`.
  - That script has the same interface as `cargo nextest run`.
